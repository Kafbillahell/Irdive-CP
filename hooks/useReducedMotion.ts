'use client';

// Re-export Framer Motion's useReducedMotion for consistent use
// Import from here — not directly from framer-motion — so it's easy to mock in tests
export { useReducedMotion } from 'framer-motion';
